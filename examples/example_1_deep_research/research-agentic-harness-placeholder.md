Empty because the wiki contained personal information.

You can easily create your own by running the `/research` skill into a harness with the `prompt.md` file as input:

```bash
/research examples/example_1_deep_research/prompt.md
```
